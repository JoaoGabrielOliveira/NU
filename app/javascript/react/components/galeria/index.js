import React from 'react'


/*
    Titulo:string
    Pagination:bool
*/
const Galeria = (props) =>
{
    let Hearder = () =>
    {
        return(
            <header>
                <h1>{props.title}</h1>
            </header>
        );
    }
}

export default Galeria;